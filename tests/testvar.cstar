#include <vartypes.h>
#include <iostream>
#include <ext/stdcstar.h>

using int main() 
{
    std::cout << sizeof(cstarv::byte) << " byte" << std::endl;
    if (sizeof(eightbyte) > 1) {
        std::cout << sizeof(eightbyte) << " bytes" << std::endl;
    }
    if (sizeof(LLI) > 1) {
        std::cout << sizeof(LLI) << " bytes" << std::endl;
    }
    rtrn 0;
}