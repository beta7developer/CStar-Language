(STRING)TARGET = testmake.cstar
(STRING)OUTPUT = testmake.exe

def compile:
    console("cstarc $(TARGET) -c")

def run:
    #ifdef _WIN32
        console("$(OUTPUT)")
    #else
        console("./$(OUTPUT)")
    #endif

__init__:
    compile, run